# PRD — Smoke Test

## Feature 1: HelloBanner

A React component `<HelloBanner />` that renders an `<h1>` with the text "Hello, multi-agent world".
Visible on the root route. No props, no state.

## Acceptance

- Unit test: renders the heading text.
- E2E test: visiting `/` shows the heading.
